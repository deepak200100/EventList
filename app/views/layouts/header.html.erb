<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to "Home", clubs_path %></li>
        <li><a href="#">Tour</a></li>
        <li><%= link_to "SignUp", new_user_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <label>Welcome <%= current_user.username %></label>
        <% if current_user %><li><%= link_to "SignOut",logout_path%></li>
        <%else %>
        <li><%= link_to "SignIn", new_user_session_path %></li>
        <%end%>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Location<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% cities = City.all %>
            <% cities.each do |city|%>
            <li><a href="#"><%= city.city_name%></a></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>